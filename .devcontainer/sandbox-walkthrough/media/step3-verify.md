## Real pass/fail, not vibes

The button runs `pixi run verify` in a terminal. It checks the gateway
connection and that the UW SSEC models are available.

Every ✗ comes with the one next action to fix it. You can re-run it anytime
from a terminal:

```bash
pixi run verify
```
