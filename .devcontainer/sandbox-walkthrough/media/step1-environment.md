## You're already set up

First launch did the heavy lifting: the **pixi** Python environment, the
**LLMoxie Model Provider** extension (routes Copilot Chat through the
LLMoxie / LiteLLM gateway), and the **Copilot CLI** with the RSE plugins.

![Codespace ready after first launch](codespace-ready.png)

> ⚠️ **Use Google Chrome.** This sandbox has been fully tested only in Google
> Chrome. Other browsers don't appear to work smoothly — a current potential
> issue with Copilot in GitHub Codespaces, not the sandbox itself. If you're
> in another browser, reopen this Codespace in Chrome.

Nothing to do here — check the step off.
