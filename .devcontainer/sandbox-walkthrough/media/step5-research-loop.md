## The guided deck

- **Research arc** (`research-loop-seismic.md`) — estimate a region's Gutenberg–Richter b-value from a synthetic earthquake catalog: `/researching → /planning-implementations → /running-experiments → /implementing-plans → /validating-implementations → /ensuring-reproducibility`.

The button opens the deck directly as slides in the Marp preview. Then open
**Copilot Chat** (`Ctrl+Shift+I`) beside it and follow the slides.
