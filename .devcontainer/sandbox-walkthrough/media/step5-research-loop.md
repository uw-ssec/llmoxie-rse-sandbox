## Two guided decks

- **Packaging demo** (`research-loop.md`) — turn the `samples/` climate
  scripts into an installable package: `/researching → /planning-implementations → /implementing-plans →
  /validating-implementations`.
- **Research arc** (`research-loop-ocean.md`) — test a warming hypothesis
  against synthetic buoy data, featuring `/running-experiments` and `/ensuring-reproducibility`.

Each button opens the deck directly as slides in the Marp preview. Then open
**Copilot Chat** (`Ctrl+Shift+I`) beside it and follow the slides.
